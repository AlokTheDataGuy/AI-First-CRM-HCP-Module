# Screenshots & video assets

Drop your captured images here so the main `README.md` renders them.

Expected filenames (referenced by the root README):

| File | What to capture |
|------|-----------------|
| `video-thumbnail.png` | A nice frame from your demo (used as the clickable video thumbnail) |
| `log-interaction.png` | The full split-screen Log Interaction page |
| `chat-fills-form.png` | Chat message that auto-filled the form (fields highlighted) |
| `edit-interaction.png` | A correction via chat updating only specific fields |
| `voice-note.png` | The voice-note recording / transcription in action |

Also add `frontend/public/logo.png` for the header logo.

Then, in the root `README.md`, replace `PASTE_YOUR_VIDEO_LINK_HERE` with your uploaded video URL.
